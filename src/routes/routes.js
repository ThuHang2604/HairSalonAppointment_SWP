import HomePage from '@/pages/HomePage';

const publicRoutes = [
  {
    title: 'Home',
    path: '/',
    component: HomePage,
  },
];

const adminRoutes = [];

export { publicRoutes, adminRoutes };
